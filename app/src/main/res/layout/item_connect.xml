<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dp" >

    <CheckBox
        android:id="@+id/connect_check"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginRight="16dp"
        android:layout_marginEnd="16dp"
        android:text="@string/list_description"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/connect_value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/connect_check"
        android:layout_alignBottom="@id/connect_check"
        android:layout_toRightOf="@id/connect_check"
        android:layout_toEndOf="@id/connect_check"
        android:gravity="center"
        android:textSize="12sp"
        android:text="@string/list_description" />

</RelativeLayout>